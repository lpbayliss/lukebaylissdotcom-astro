---
export interface Props { title: string }
const { title } = Astro.props as Props;
---

<section>
  <h1 class="capitalize text-xl font-semibold mb-10 text-headline dark:text-headline-dark">{title}</h1>
  <slot/>
</section>